services:
  - type: web
    name: fintrack
    env: php
    plan: free
    buildCommand: composer install --no-dev --optimize-autoloader
    startCommand: php artisan config:clear && php artisan cache:clear && php artisan storage:link || true && php artisan migrate --force && php artisan serve --host 0.0.0.0 --port 10000